# Скопируйте в .env рядом с docker-compose.greenmask.yml и заполните реальным токеном
# Токен НЕ должен содержать слово "Bearer" - только сама строка
N8N_WEBHOOK_TOKEN=
PGHOST=127.0.0.1
PGPORT=5432
PGUSER=root
PGPASSWORD=#######
PG_SOURCE_DB=sanitize_demo
PG_TARGET_DB=sanitize_demo_target
N8N_WEBHOOK_URL=https://dbbatyln8n.ru/webhook/########
